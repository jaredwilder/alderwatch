# GitHub / regular ChatGPT handoff — 2026-09-07

- Prepared the existing game for the user-specified public repository `jaredwilder/alderwatch`, initially empty. No gameplay or save changes in this packaging pass.
- `README.md`, `HANDOFF_CHATGPT.md`, asset credits, and push/PR test-build CI added. Standalone handoff also saved outside this repo as `../Alderwatch-Sol-Handoff.md`. Target is regular ChatGPT Sol on High with the user's existing GitHub plugin; tools must be checked, not assumed.
- Runtime GLBs/WebPs, source textures and approved concepts are included. Large `assets/source/` authoring masters/downloads remain intact locally and are ignored, as are dependencies, build output, most old captures and credential-file patterns. This repository is not a complete Blender-source backup.
- Fresh local verification: all 78 tests passed; production build passed with existing ~3MB bundle warning. No new visual/playtest acceptance claimed. GitHub CI outcome should be read from the actual run.

# Seeded frontier expansion — 2026-09-07

- User requests a much larger, populated procedural realm. Implemented finite768x768m terrain footprint (previous520x520). Original mathematical heights unchanged inside old±260 boundary; outer rise beyond300m forms steep rim. Northern authored highland remains scenic backdrop; expanded populated exploration is south/east/west, not an infinite fully streamed terrain system. Landscape ground/collision remains one finite heightfield mesh. Regional topology is shared; saved seed varies site jitter, resource distribution and local vegetation, not the legacy terrain function.
- `src/worldgen.ts`: version1 additive metadata, persisted worldSeed, existing-realm fallback197709; new games explicitly created through normal UI get crypto-generated seed. Three regions: Southwood (denser woodland), Ironward Heights (more rock), Briar Heath (open heath). Default isolated generator718 harvestable resources,179 forage,6 extra hare/crow; counts vary with seed and occupied saved land. Existing buildings, resources, dead enemies, containers and progress never overwritten. South road and east–west dirt trail connect regions and appear on minimap. Generator returns untouched on existing frontier metadata.
- Six generated sites: Southroad Watchfire, Lost Timber Stockpile, Ironward Toll Camp, Ridge Prospectors, Briar Supply Hollow, Heath Reavers. Two guarded camps with two enemies each; rest stops with usable campfire/workbench; chests with real iron/hide/stew stacks. Uses existing authored kit, not new asset models. Guarded cache authority covers new sites. Journal Open Frontier section tracks each site through saved `track_frontier` command and existing minimap; bounty/expedition progress retained. Map canvas covers enlarged footprint, new trails/site markers. Finite loot, not repeatable/infinite rewards.
- `FrontierRenderer`: tiled instanced distant resource proxies, authored interactable resources/colliders inside38m, nearby3x3 32m vegetation chunks. Streaming updates every.4s, releases instance buffers on unload/realm rebuild; build changes rebuild nearby cover. Proxy vanishes for harvested resource and does not resurrect; existing falling-tree and drop physics retained. Original population loops exclude generated resources so reload does not double-place them. Nature renders forage only near player. Regional vegetation tint and density variation; still existing grass/oak art limitations.
- Build passes (existing~3MB chunk warning),78/78 tests pass. Tests cover same-seed equality/different-seed variation, populated sites, terrain height preservation, build avoidance, saved depletion, guard authority, tracking and real Rapier resource collider unload/reload/non-resurrection. Live separate `?rehearsal=frontier` (explicitly NOT SAVED): walked ~24m over generated ground, remained grounded, opened watchfire chest with actual6 iron/4 hide/2 stew; chopped generated oak wild-resource-312 from6HP to0 and physical falling phase using normal held axe input. Journal shows all six trackable destinations. Final screenshot `art/captures/procedural-southwood.png`; observed84–120FPS snapshots at1280x720, not sustained benchmark. Entire expanded perimeter and both new camp fights not manually traversed. Known architecture merge/color gpuType warnings predate this turn; no new shader errors observed.
- Tests ran in new isolated frontier tab; user's existing normal and nature-rehearsal tabs not controlled. No tests/grants/teleports changed normal saved realm. Art parity still unresolved; this is content/world-generation expansion.

# Minimap and objective navigation — 2026-09-07

- Added `src/minimap.ts` and responsive `minimap.css`: lightweight 10Hz canvas minimap, cached terrain/pond/road base, nearby trees, player construction, workbenches, home marker and nearby living enemies. Camera-up: W travel corresponds to up; white arrow is actual character-facing and N rotates around rim. Gold objective diamond/dotted bearing line becomes an edge arrow when off-map, with explicit Ahead/Left/etc and metre distance. Follows the same tracked bounty/Ash Road objective as HUD; no pathfinding or autorun, line is direction only.
- Mounts on each gameplay HUD rebuild (menus remove it); hidden/non-connected canvas not updated. Narrow-screen layout keeps it available even when legacy quest/compass are hidden. No save schema or combat changes this turn.
- Build passes (existing JS chunk warning);74/74 tests pass. Projection tested at four cardinal camera headings, edge clamping, zero-distance and directional labels. Live screenshot verified compact top-right layout; turning camera from34° to90° changed objective Ahead-left→Left correctly. Capture `art/captures/minimap-navigation.png`.
- User appears to have begun moving/playing in the visible `?rehearsal=nature` test tab during QA. Stopped control and did not close it; explicitly warned it is NOT SAVED and directed Back to saved realm. Main `/` tab retained too. Do not assume preview progress is saved or silently move it into user's realm.

# Combat responsiveness and bounty camps — 2026-09-07

- User requests content and repair of weak reach, enemies outrunning swings and stopping to attack. Found actual one-frame zero-target velocity at each attack expiry (floating-point residual `locked`); epsilon clamp removes it. Light moving attacks now retain normal walking speed (3.2m/s, up to4.3 when sprint held); heavy2.7. Existing upper-body/leg layers and authored contact times/socket remain intact. Enemy locomotion multiplier.78 keeps approach below player walking speed.
- Moving swings no longer overwrite windup-assisted facing with camera yaw. Nearby forward, unobstructed enemies are selected; windup can track at8rad/s until the existing pre-impact cutoff. Light sword reach1.95/fine2.05, axe2.2/pick2.1; light sword capsule corridor.85, heavy unchanged. Shorter light recovery,22-tick click buffer, held LMB chaining. Authoritative hit windows, single-hit consumption, barriers, rear misses and stamina costs preserved. Enemy close-range dead zone and guarding toward the sidestep rather than player corrected. Less knockback and less circling during recovery; timed group beats retained. Damage toast added.
- New additive `src/bounties.ts`: two optional journal contracts, The Woodcutters’ Ransom (one60HP deserter; sword or4 extra iron,2 grilled venison,6 iron), The Poachers’ Hollow (two78HP poachers;12 iron,8 hide,2 broth). Journal tracking drives HUD bearing and activates defenders; current pursuers do not disengage just because tracking switches. Reuses authored palisade/chest/fire meshes, terrain-safe site selection avoids construction/resources/stations; clear chest areas. Authoritative reward requires tracking, reach and every defender dead, pays once; dead enemies/emptied camps/progress preserved. These are two finite contracts, not repeatable quests or new enemy species. Existing Ash Road untouched.
- Build passes (existing ~3MB JS warning),73/73 tests pass. Added regression coverage for full movement through attack expiry, held chains, windup facing, light reach/rear/distant misses, both contract rewards/save idempotence. Live isolated nature fixture: axe attacks landed20 damage; moving-swing segment covered ~4.2m in1.5s, enemy countered, held attacks killed; walked to chest and E claimed actual sword/food/iron, journal showed completion and second contract. Screenshot `art/captures/bounty-complete.png`. Second camp full manual fight not completed; two-defender reward gating automatically tested. Normal save not used for testing. Asset quality remains below references; no art-parity claim.

# Elevated action-RPG camera — 2026-09-07

- Latest user wants Diablo-like pulled-back/top-down framing. Default pitch now .96 radians (55 degrees), distance14m, gameplay FOV42; zoom7–24m and pitch.45–1.3. Mouse inversion signs retained. Cinematic title/creation retain FOV48. Intro orbit uses the same spherical offset as gameplay.
- Added foreground tree bark/leaf sightline cutaway with feathered dither and near-camera leaf clearance; camera initially sat inside the oak canopy, verified and corrected in-browser. Shared uniforms follow actual projected player; no collision/state/asset changes. Cutaway disabled for cinematic menus, shadows retained. Roof occlusion remains the existing collision push-in, not an interior cutaway system.
- Build passes (existing ~3MB chunk warning);69/69 tests pass. Live non-saving nature fixture: sprint moved16m, camera pitch.96/FOV42/distance14 verified, wheel moved distance14→14.85. Actual capture `art/captures/elevated-action-camera.png`. Browser returned to normal `/` title with Continue available; save unmodified by testing. Vite5190 was stopped and restarted (no duplicate listener).
- Console exposed existing geometry batching errors about color BufferAttribute gpuType mismatches; no shader compilation errors observed. Not repaired in this camera-only pass. World/character art still below approved reference; this is a framing/readability improvement, not art-parity acceptance.

# Nature, moving swings and camera — 2026-09-06

- Added original Blender-authored nature kit (`scripts/author_wildlife.py`, `wildlife.glb`, 380,288 bytes): articulated hare/crow, berry bushes, mushrooms, herbs and fallen branches. Separate `AW_NATURE` collection, 18 objects, 8,630 vertices. Source .blend saved and MCP claim released. Export initially included another selected scene and lost viewport colors; corrected active-scene-only export and promoted authored vertex colors during asset preparation. Repeated authoring now preserves object identities for the MCP context guard. Existing character and frontier-kit exports unchanged in size.
- `src/nature.ts`: two hares and two crows wander/flee with articulated limb/wing motion. Ambient animals only, not huntable enemies yet. Additive persistent animal state; authored forage placed beside roads/clearings, hidden around buildings. Berries/branches give existing food/timber; mushroom/herb stacks make woodland broth (+40 stamina, eight minutes). New forage regrows after five minutes of active simulation; legacy gathered flax remains gathered. Authority validates reach and duplicate harvesting. Q supports broth.
- Swing locomotion: upper-body moving variants retain the original attack/tool tracks; independent walking lower-body layer drives legs. Light sword movement 2.55m/s, tools1.8, heavy1.35. No sprint while attacking; hit/death/build locks remain. Moving swings face camera aim, stationary auto-aim retained. Live browser check: axe moved ~1.4m over .85s, `chop_moving`, exact grip/tool world-position match. Authored stationary contact tests still pass.
- `src/follow-camera.ts`: mouse orbit no longer passes through slow position smoothing; translation follows separately (vertical damping reduces terrain bob). True spherical pitch, wider look range, three collision clearance rays and eased outward recovery. Accepted yaw/pitch signs unchanged. Journal Accept and current next-step text moved above map.
- Build passes; **67/67 tests pass**, including moving sword/axe/pick contacts, nature export, additive regrowth/cooking, camera response/collision release, all previous combat/build/save tests. Browser screenshot verified brown hare and crow in actual game; `art/captures/nature-moving-swing.png`. Disposable `?rehearsal=nature` is non-saving; no testing grants or teleports touched the normal realm. Main realm tab restored at handoff. Animal models are simple authored wildlife, not a claim of finished/reference-quality animal art.

# The Ash Road playable expedition — 2026-09-06

- User asked for a bigger chunk of gameplay/fun after accepting the visual progress. Added The Ash Road: J/M field journal, schematic route map, objective bearings, three persistent encounter sites (one axe scout, two sword guards, Edric the Ash Captain), guarded dispatch recovery, and explicit hand-in at Alderbrook's workbench. Four new opponents reuse the authored adult skeleton, clips and hand sockets; camps reuse the authored palisade/chest/fire kit. No new concept art or Blender source changes this turn.
- Additive site/enemy/container seeding avoids standing resources and player construction; existing/dead registry entries and recovered rewards never reset. Each player owns accepted/recovered/completed flags. Three recovered dispatches survive death. Hand-in awards +15 max health/stamina and +10% melee damage, 12 iron, 6 hide, 3 stew and a tempered sword only if missing. Guards cannot be kited away to bypass cache checks. Full hand-in/reward/save/reload/idempotency verified through authority tests, not yet a complete manual browser hand-in.
- Q eats available prepared food, prioritizing missing buffs, with a persisted ten-second cooldown. Campfire menus offer safe rest for one timber (blocked near living enemies). Slot 3 correctly selects/displays the owned tempered sword. RMB raised within 12 ticks of a light hit parries for 8 stamina and staggers the attacker; heavy cuts cannot be parried. Nearby guards reserve attack beats at least 100 ticks apart after browser testing exposed overlapping-hit interruption pressure. Captain alternates authored heavy/light cuts; heavy windup has an explicit HUD warning.
- House-entry blocker fixed using two visible reused-foundation threshold steps and matching colliders, plus 0.24m controller autostep. The unchanged entry/floor-height/closed-door/roof/crafting/storage assertions pass. The side-wall check now walks clear of the chest before checking the wall (the previously failing entry had prevented that assertion from ever running). Village's separate authored porch geometry/colliders unchanged.
- Build passes (existing ~3MB JS chunk warning). All **62/62 tests pass**, including live Rapier AI attack spacing, captain skeletal heavy clip/socket, timed parry, food/rest validation, additive save migration, ordered guarded dispatch recovery and one-time hand-in rewards. Build/tests are not commercial visual acceptance.
- Disposable browser fixture `?rehearsal=expedition` is explicitly labelled NOT SAVED and never writes the user's realm. Actually played: accept journal, Q food, walk to scout, defeat/recover; defeat both supply guards with revised timing, recover supplies; restore 170 health at captured campfire for one timber; approach and defeat captain. First attempt at the pair exposed stunlock and was corrected/retested successfully. Final browser run paused near captain strongbox; background-tab throttling hindered final pickup/return QA. All three-site hand-in logic tested automatically. No grants, teleports or progression changes made to user's normal save.
- Remaining scope: no repeatable contracts, new enemy species or multiplayer this turn. Captain shares the humanoid rig/outfit and can still be pressured by repeated light strikes; more distinctive boss tactics/art remain useful. Overall character/environment reference-art mismatch from prior checkpoint remains unresolved; this was a gameplay chunk, not an art-parity claim. Free-look signs remain the user's accepted yaw += dx*.003, pitch += dy*.002.

# Village structural replacement — 2026-09-06

- Latest user request: flip up/down again and materially improve visuals. Current look signs are **yaw += dx*.003; pitch += dy*.002**. Horizontal unchanged from prior accepted inversion. Do not flip either again without user direction.
- Rebuilt village silhouette using new source `scripts/author_village_meshes.py`, invoked by house authoring: `village_roof` continuous bowed/thick 7.38m-wide thatch mantle, `village_gable` 6m loft facade/window/framing, `village_details` 6.9m masonry chimney, off-centre porch, three entry steps, firewood store, barrel/chopping block. Old player-building modules remain unchanged and exported.
- Landscape assembles 6x6m cottages (four original foundation modules) with new roof/loft/details and actual wall/roof/door/steps/chimney/storage/post colliders. `wideCottage` retains legacy narrow cottage if expansion overlaps saved structures or standing tree trunks. Cached per realm build. Grass/rubble clearance follows selected footprint. No player progress changes, resource grants, teleports or auto-completion of user buildings.
- Static new cottage meshes are consolidated per material after local transforms, leaving colliders independent. Actual view draw calls fell ~750 to523; measured snapshot79FPS at1498x1272, not a sustained benchmark. Final image `art/captures/village-structural-rebuild.png` shows actual saved gameplay, new broad houses and their material weathering. Several new cottages are visibly in use; no assertion of reference parity.
- Generated original aged plaster with built-in image generation, inspected and applied directly to all AW_plaster surfaces, including user modules. Source `art/direction-v2/plaster-albedo.png`, runtime `public/textures/plaster.webp`, exact prompt/provenance `art/direction-v2/PLASTER_PROMPT.md`. Prep script includes it. Source image original retained under .codex/generated_images.
- Final kit7,950,012bytes (still below unchanged8MB test limit); survivor3,282,748bytes unchanged. Blender source saved and claim released. Build passes;53tests52pass,1known original house-entry failure. Village mesh export/UV/scale test added. Existing doorway-controller gap not fixed; new porch walkability still needs end-to-end verification.
- Still unresolved: stylized character face/anatomy and outfit do not match adult concept; tree/terrain composition, repetitive chimney masonry and distant mountain shapes remain visibly simplified. This is a structural village improvement, not delivery of the full approved visual target. Prioritize real modeling/composition work over further tiny tint adjustments.

# Free-look and visual-quality correction — 2026-09-06

- User requested mouse free-look without holding RMB, then explicitly requested inversion because it felt reversed. `src/input.ts` now captures pointer during play, releases for menus, supports hover-look if capture is denied, suppresses attacks on recapture, clears stale deltas/held guard, and keeps build UI cursor usable. Both camera axes now inverted from the initial free-look implementation: yaw += dx*.003; pitch -= dy*.002. RMB remains guard. Seven real Input event-handler tests added; all pass. Full suite51/52, existing house-entry failure unchanged; build passes.
- Runtime grass canopy normals are upward-biased with correct double-sided fragment orientation. The initial version incorrectly inverted back-face lighting and was fixed after screenshot inspection. No Blender edits this turn. HUD/settings controls updated; read_environment now includes actual controls/capture state.
- User says gameplay still looks nothing like approved concepts and only the title composition looks good. This is a fundamental asset/modeling/composition shortfall, not a request for more tint/noise passes. Prioritize replacing the stylized adult character and generic architecture forms, plus deliberately composed terrain/understory. Do not equate extra polygons, texture swaps, shaders, tests or a flattering title camera with actual visual acceptance. Reference parity has NOT been achieved.

# Visual polish / building-depth pass — 2026-09-06

User still rejects current visual quality, specifically boxy unfinished-looking buildings. Keep visuals above new features; no commercial/reference-parity claim.

- House-family meshes now have subdivided, subtly undulating smooth-shaded lime plaster, vertex-painted recessed edge/foot staining, hand-hewn irregular timber arrises, knee braces/pegs, projecting weatherboards, dressed stone window sills and less uniform masonry. Ragged straw ends soften roof eaves. Preserve existing actual openings, foundation height, snap grid and user-placed pieces; no automatic completion of the user's roof.
- Cowl refitted into a narrower folded scarf with pointed front drape, not a circular poncho. Rig/actions/socket identities unchanged. Flax's broad polygon wedges/white bouldery buds replaced by narrow lanceolate leaves, fine stems and five-petal blue flowers. Ferns preserved.
- Grass has broad low/tall patches, world-space olive tint variation and darker bases; extended footprint with gradual edge taper avoids the old rectangle boundary. Same terrain/resources/player state. Softer shadows, warmer lower-angle sun, stronger cool fill and distance haze improve depth. No postprocessing dependency added.
- Close-up QA caught square plaster normals and white-hot iron pegs; corrected smooth normals/darker iron. Kit6,540,640bytes; survivor3,282,748bytes. Blender source saved and claim released. Build passes;44/45 tests pass, known house-entry failure unchanged.
- Remaining important gaps: character face/anatomy and materials still stylized versus reference; mountain forms still simplistic; village/forest composition still repetitive. Do not call art finished. Player actively explores between checks; no grants/teleports/build changes by agent. Updated gameplay capture: `art/captures/visual-polish-buildings.png`.
- Final grass correction tilts each patch to its terrain normal and buries the opaque atlas base .10m; minimum height raised to avoid visibly flattened card edges on slopes. Final build passes. Final capture shows smooth plaster/sills/thatch; user independently equipped a fine sword and advanced quests during this turn. Agent only rotated camera for the capture.

# Approved art replacement sweep — 2026-09-06

User approved the complete seven-image set and asked for a token-efficient replacement sweep. Applied it in the existing Alderwatch project, not WooCommerce. Full game goal remains active/incomplete; this is not a claim of concept-art parity.

- Actual character geometry: `scripts/apply_character_art.py` adds a skinned folded shoulder cowl, split jerkin hem and cropped beard; broadens torso/legs/jaw modestly, reduces oversized pauldron, removes second decorative belt from export. Noncumulative `AW_ArtV2Base_*` mesh backups retained. User hood/hair/skin choices unchanged. Existing skeleton, 14 actions, Grip_L/R and quaternion-continuity repairs preserved.
- Approved wool/leather images are converted to runtime WebP and mapped to new costume meshes; original Ranger atlas green regions receive the generated surfaces while retaining seams. Fixed material dedup merging three default Blender shader materials: the optimizer now preserves survivor surface extras and explicit costume color factors. Regression tests check material identity, UVs, weights and skin bindings.
- Approved grass atlas and meadow albedo now ship. Deterministic .52m jittered grid with overlapping 1.1–1.48x grass patches replaces random sparse sprinkling. Terrain soil blend is confined chiefly to paths/root zones. Existing fern asset retained, along with road/building clearances and saved-resource random sequence.
- Forest family: broader crooked oak branches and longer curved buttress roots. Architecture family: cream weathered plaster infill, dark timber framing/diagonals, preserved real door/window openings, plaster gables and existing thick layered thatch. Existing foundations, furniture, hand tools, limestone and fern assets preserved. No unrelated gameplay feature changes.
- Blender source saved to `assets/source/alderwatch-assets.blend`; instance released successfully. Shipping survivor 3,282,744 bytes; kit 6,357,708 bytes. Final costume fitting was read back before export. One script runner variable collision interrupted the export stage after fitting; inspected cowl, then ran only the export stage successfully. No uncertain partial state remains.
- Build passes; **45 tests:44 pass,1 unchanged known house-entry failure**. New costume/house material tests pass; combat, animation continuity and socket regressions pass. Existing ~2.98MB JS chunk warning remains.
- Actual normal-save browser capture: `art/captures/approved-art-sweep.png`. Final measured snapshot144FPS at1498×1272,402 draws/~2.09M triangles; not a sustained benchmark. Browser is left in the user's existing gameplay save. No grants, teleport, rehearsal save replacement or agent-attributed gameplay progress.
- Visual gaps remain: head/anatomical detail still noticeably stylized versus the rugged adult turnaround; cowl is relatively broad, environment/trees and distant mountains still show procedural repetition, and the full settlement prop sheet is not completely rebuilt. The sweep is applied, but do not call it commercially finished or pretend screenshots match the reference exactly. Keep visual quality ahead of new features.

# Previous checkpoint — ART SET FIRST, 2026-09-06

Latest request supersedes the feature/house-physics work: stop piecemeal edits; generate a full coherent art set FIRST, then apply it together. User explicitly likes the new ferns, rejects sparse grass and current character art. Character artwork and dense ground cover lead the unified rebuild. The seven-image package is now generated, viewed and saved under `art/direction-v2/`: `character-turnaround.png`, `forest-kit.png`, `settlement-kit.png`, `grass-atlas.png`, `meadow-albedo.png`, `wool-albedo.png`, `leather-albedo.png`. `ART_SET.md` is the gallery and application brief; `PROMPTS.md` contains exact built-in generation prompts. **None of this v2 art set is applied in-game yet.** No further runtime changes were made after the user's art-first request. Next goal turn should apply the coherent set, starting with actual character mesh/clothing/face (not merely a color tweak), then dense continuous vegetation, then the environment/architecture forms. Do not generate another plan or falsely call the existing character matched to the sheet.

Immediate source checkpoint before art-set generation:

- Retargeted animations now use hemisphere-continuous quaternion keys sampled every frame with LINEAR interpolation. This fixed measured idle hand spikes from39.7m/s to0.11m/s; walk40.3→.96, sprint48.9→4.25. `scripts/audit-motion.ts` audits shipping clips. Two new integration tests verify arm continuity and a non-overhead axe contact. Sword timing/socket tests still pass.
- `author_tool_clips.py` now bakes compact lateral chop/mine wrist/shaft targets every frame (38frames/37÷30duration, contact17÷30). Combat quaternion keys also preserve sign continuity. Correct regeneration order is retarget_character → author_combat_clips → author_tool_clips → refine_character → prepare-assets. Preserve all backups and verified sockets. Survivor3,261,752bytes/14clips.
- Forest kit now30assets/~6,448,632bytes. New textured curved fern cards (accepted by user), angular rock forms, shallow `paving_0` flagstone, heavier curved oak boughs, directionally vertex-lit highlands. Last Blender source saved; claim released. `art/textures/fern.png` is built-in generation with prompt/provenance in `art/textures/prompts.md`; keep it.
- Runtime has denser/taller but STILL TOO SPARSE grass per user, broader meadow/soil blending, shared exp2 haze, darker stone, weathered umber character shader, 4096shadow map and explicit shadow-camera projection update. These are insufficient; user requests coordinated art overhaul, not more tiny adjustments.
- Latest `npm test`:43total,42pass,1known house-entry integration failure. Do not hide/weaken it. Actual Rapier survivor oscillates outside .44m foundation threshold with open door; paused in favor of user's visual priority. All combat/animation tests pass. Build passes, existing~2.98MB chunk warning remains.
- Prior house pass introduced shared `structure-geometry.ts`, two-bay `development-fixtures.ts`, `?rehearsal=house` isolated non-saving realm, automatic roof gable joins, real sloped roof colliders and swinging door colliders, support-removal rejection. Source house authoring lives in `scripts/author_house_meshes.py`, called automatically by full environment generator. It is not fully validated as walkable.
- Browser was normal `/`; user actively played during this turn. Never attribute their progress to agent tests. Reloads preserve normal save. Most recent page was title after completed asset export; Continue should restore user save. Preview showed144FPS at1544×1272, not a sustained1080p benchmark. No live grants/teleports or save replacement. `art/captures/visual-priority-before.png` is a real before capture. A final accepted after image does not yet exist.

Full goal remains active and incomplete. Do not revert to obsolete Blender axe setup or the unrelated WooCommerce project.

# Prior checkpoint — world surface / highland repair, 2026-09-06

The full Alderwatch goal is still active and **not complete**. This is concrete progress, not a release acceptance claim. No changes were made to WooCommerce.

- Read the full objective, AGENTS, GAME_VISION and VERTICAL_SLICE again. Compared live screenshots against `art/concepts/hero.png`. Repaired actual source meshes and runtime, iterating screenshots multiple times.
- `scripts/build_environment.py`: local transported radial frames and arc-length UVs for bark tubes; face-loop seam UVs and end caps; length-aligned grain for timbers. Narrow paired fern pinnae replace broad leaf wedges. Rock meshes have more vertices and smooth normals, but the result looks too rounded/potato-like and needs an authored rock-shape pass.
- Added authored `oak_distant` (~1/10 near-tree vertices) and connected `highland` mesh (26,245 vertices, 51,840 triangles), asymmetric spurred peaks and colored stone/vegetation slopes. The old three detached sinusoidal strips are removed. Kit now28 assets and7,313,196bytes optimized. Survivor remains3,244,492bytes, unchanged14clips and verified sockets.
- First mesh rebuild actually exported and saved, but its MCP context restoration reported a removed Object reference. Readback and timestamps proved all28 assets existed. Fixed the generator to **reuse Object identities** while replacing their mesh data, instead of deleting the named collection's Objects. Subsequent full rebuild/export/save succeeded cleanly. Source `.blend` saved11:13:46AM, active scene Alderwatch Kit, claim released successfully. Preserve unrelated character/source scenes. Blender instance/file-session unchanged.
- `src/ecology.ts`: deterministic noise, meadow density, backdrop-height function matching Blender. Distant tree placement now checks the bounds of the actual playable terrain; it no longer evaluates an invisible infinite heightfield outside those bounds. A shipping-GLB test checks sampled highland vertices against this formula to.002m. Added woodland slopes, no distant-tree shadows; chunked instancing (24m ground cover,72m distant trees) permits culling.
- `src/ground-material.ts`: actual meadow/soil albedo splat blending from road and saved-tree positions; no playable heightfield changes. Landscape now populates resources before authoring terrain masks so new and saved realms both have root zones. New built-in-imagegen soil source is `art/textures/soil.png`, optimized `public/textures/soil.webp`; exact prompt and provenance in `art/textures/prompts.md`. Other texture sources remain preserved. Grass is shorter with variable height/density; leaves and grass use alpha-to-coverage. Ground still looks repetitive at mid-distance; no claim of concept parity.
- Default gameplay camera pitch.03 instead of.24 exposes the horizon. Camera far1800/sky scaled3 for authored highlands. Scene fog95..480, highland has vertex color and stone map but fog disabled/color(.42,.52,.66). **Distant lighting remains mismatched:** mountains are too dark/plain relative to the pale distant trees. Fix shared atmospheric perspective and layered rock forms, not just exposure. Foreground trees still have repetitive crowns/branch joins; next major priority is attractive connected building kit and village/home composition.
- Added read-only `read_environment` WebMCP with actual camera/bounds/material state for QA. Existing game tools remain. **38 tests and build pass**; JS chunk warning~2.97MB persists. Tests do not prove complete visual or gameplay acceptance.
- Actual saved1080p capture: **`art/captures/world-surfaces-2026-09-06.png`**. It is normal gameplay, not a render/rehearsal. Compare against hero: terrain has soil root zones, bark alignment fixed, finer ferns, mountains/woodland now continuous, but buildings are tiny/simple/open gables, rocks rounded, ground cover still visibly tufted, background lighting not coherent. Full opening sequence not yet verified.
- Current normal save had already progressed through user actions before this turn:59HP,hooded Warden,hammer,wood3,stone18,iron10,hide3,venison3,berries3,home[33,1.4629,-21],combat5/building4/gathering30. Dead outcast visible. Do not attribute these gains to agent tests. Agent only performed short ordinary A/W movement and camera checks; inventory equality verified. Final player~[18.374,1.215,-17.953], facing north. No teleport/grants/rehearsal/save replacement.
- Preview timing this turn remained~1Hz, even though visibility/focus read true. Last1080p metrics CPUupdate.68ms/render submission2.43ms,353draws,2.287mtriangles,75textures. **60FPS remains unverified**. Ordinary3.5-second input moved only~.6m because frame dt is clamped under slow scheduling. Do not call this successful sustained movement/performance QA, or restart a live server because of it. Continue other meaningful work if preview stays throttled.
- IAB still browser1/tab1/provider`c401bf09-a641-41ab-ad26-5917159bb1a1`, normal`/`, Continue loaded, marked handoff. Temporary1080p override reset before handoff. Vite session7558 remains the existing server; verify handle before restarting. Browser logs have historical missing-highland error18:03:44UTC (HMR before asset export) and texture Event18:09:50UTC (HMR before soil compression); current rendered page has no later errors.

Continue with the full prior remaining gates below: building kit/home walkthrough, world art repairs, creation/turn clips/armor/progression/map/wildlife/audio, complete opening sequence and persistence/performance validation. Do not revert to the obsolete Blender test or mark the game complete.

# Previous checkpoint — contact-matched combat, 2026-09-06

The active goal remains the full **Alderwatch** vertical slice in `GAME_VISION.md` and `VERTICAL_SLICE.md`. It is **not complete**. This section supersedes the older checkpoints below; do not return to the obsolete Blender setup/axe test.

## Latest authoritative progress

- Implemented `combat-rules.ts` and `combat.ts`: stable enemy registry, alert/approach/circle/windup/recovery/stagger/death AI, actual Rapier character controllers and the existing animated adult rig, timed single-use strikes, stamina, directional guard, middle-window dodge immunity, hit cancellation, knockback, particles/sound, death and manual persistent drops. One 88-HP outcast at the eastern camp. The cache rejects opening/transfers while guarded. Player death retains tools/food and drops gathered materials; explicit respawn returns near home or spawn.
- Sword light damage24, contact13/30 seconds, recovery31/30, range1.6m. Fine sword34/range1.7. F performs a two-handed heavy cut: 41 damage with the basic sword,26 stamina, contact25/30, duration47/30,32 guard stamina pressure. Short pre-contact aim tracking and a narrower heavy blade corridor prevent wide phantom hits. Physics line-of-sight rejects strikes through barriers. Static camp palisades/workbenches/chests now have physical colliders. Ambient cottage colliders are still missing.
- Blender inspection proved the previous stock Sword_Attack contact occurred before the old damage event. `scripts/author_combat_clips.py` now authors **attack, heavy, guard** on real anatomical arm chains with retained fake-user Action backups. Raised two-hand guard and distinct overhead heavy are visible in the browser. Source saved at `assets/source/alderwatch-assets.blend`; claim released. Source instance/file session unchanged.
- A shipping-GLB integration test exposed a one-frame NLA export offset. `prepare-assets.mjs` now normalizes every animation to zero. Do not undo this: it aligns exported poses with contact times, including chop/mine. Survivor now14clips/~3.24MB; kit26assets/~5.37MB. Correct rebuild order if redoing all animation: retarget_character → author_tool_clips → **author_combat_clips** → refine_character → prepare-assets. Do not run the obsolete export_character script.
- `Character` now obeys the authoritative action window rather than separate damage timing. A composed skeletal `guard_walk` combines authored guard upper-body tracks with walking lower-body tracks; feet animate while the sword stays raised. The outcast's cloth palette is recolored to muted oxblood in its PBR material, preserving leather/metal detail. Actual screenshot confirms this identity.
- **35 tests pass**, including headless tests loading the actual shipping GLB geometry, rig and animations (GPU textures omitted only for Node): blade position at light/heavy damage frames, exact socket attachment, real AI/controllers landing strikes, physics barrier occlusion, walking guard pose. Full build passes; main JS~2.97MB/~1.04MBgzip warning remains.
- Actual browser evidence: old rehearsal recorded four24-damage hits, bandit death, manual pickup and camp chest access. These actions occurred outside agent inputs and were reported as observed user activity, not agent tests. Agent subsequently tested held guard reducing17→2damage and costing18stamina, then a41-damage heavy counter reducing88→47HP and triggering the skeletal hit/stagger. The subsequent narrow-corridor/tracking refinement passes tests; it still merits another full player-controlled duel.
- Actual final image: **`art/captures/combat-guard-2026-09-06.png`**. It is a paused, isolated gameplay rehearsal, not a staged Blender render or proof of the whole opening sequence. It shows raised guard, outcast windup and the eastern camp. Compare with `art/concepts/hero.png`: the world is still clearly below target (flat/repetitive ground cover, crude fern sheets, empty slopes, simple/suspended-looking mountains, primitive-looking wood/stone props, insufficient atmospheric/world composition).
- Dev-only `?rehearsal=combat` creates a disposable in-memory combat fixture. **It never saves over the normal realm.** Persistent top-right controls pause/resume/restart/return to saved realm. WebMCP `combat_input` accepts attack/heavy/guard/dodge and optional `pauseAfter` for this rehearsal only; `read_combat` includes30 recent events, AI phase, health, animation and sockets. Normal game input APIs remain real controls; no live-save teleports or grants. Pausing freezes the actual character pose for screenshots.
- Browser was restored to normal `/` and Continue. Verified original hooded Warden at approximately[-8.729,1.945,1.309],110HP,24wood, original tools/food, gathering18, no combat skill gain from rehearsal. Never copy rehearsal state into that save. User may play alongside the agent; inspect and avoid conflicting control.
- Previous Vite session10175 was genuinely missing and no5190 listener existed. Started a new server: **exec session7558**. Verify it before any restart. Old browser logs retain17:29 websocket/disconnected-server errors; those are historical, not fresh runtime failures. Browser reloaded successfully after restart; HMR works again.
- Latest live performance sample after1.6seconds of the combat rehearsal:143FPS, CPUupdate.47ms/render submission1.59ms,176draws,1.48mtriangles, **actual1920×1080**, focused/visible. This is a short sample, **not sustained60FPS acceptance**. Earlier1FPS samples were background scheduling; lowCPU did not prove GPU performance. Temporary viewport override reset.
- Current IAB mapping is **browser1/tab1** (changed from prior tab2 across continuation), providerTabId remains `c401bf09-a641-41ab-ad26-5917159bb1a1`. URLhttp://127.0.0.1:5190/, marked handoff. Use inventory if local IDs change, not a duplicate tab. CUA bindings currently tab/simple, browser, game/advanced, gameTools, captureFs. Refetch tools only on document change/stale notifications.

## Continue next, keeping the full goal

1. Repair world art against concepts: terrain/mountain silhouette+base seams, mature-tree UVs and foliage layering, patchy unnatural grass distribution, bright primitive ferns, authored natural rocks/road, richer camp/village/home construction. The current actual scene is not visually close enough. Use Blender/imagegen where appropriate, with real screenshot comparisons.
2. Finish a real attractive small house and verify roof/door/floor/collision/storage/dismantle/home-workbench flow in the normal game without replacing the user's save. Previous construction evidence is partial. Align build instructions that still say “X dismantle nearest” with current pointed-piece selection.
3. Finish creation body/face choices, actual turn clips, gear/armor/quality/salvage, map/proficiency/progression, wildlife/landmarks, more credible audio. Add camp-looted/return-home progression. The broader product spec is not satisfied merely by the combat milestone.
4. Verify normal gathering→crafting→fight→cook→house→better gear→camp→return sequence; check exact axe/pick contact now that clips are zero-based, sustained1080p performance, death/respawn/save reload and mid-fall persistence. Do not mark the full goal complete from passing tests.

# Previous checkpoint — crafting/building continuation, 2026-09-06

The goal is still **Alderwatch**, the commercial medieval survival RPG. The full objective has not changed or been completed. Read `GAME_VISION.md` and `VERTICAL_SLICE.md`; never return to the obsolete Blender connection/axe test. This project remains separate from WooCommerce.

## Latest implemented and verified work

- New `definitions.ts`, `economy.ts`, `building.ts`, `game-panels.ts`, and shared `terrain.ts`. Main runtime wires C crafting, B building, E station/chest/door/flax interactions, inventory equip/eat/drop/reorder, storage transfers, food status and real maximum health/stamina. Construction uses authored meshes, valid/invalid material previews, 3m floor grid, edge sockets, quarter-turn rotation, supporting walls for roofs, ownership/material/reach checks and persistent structures/stations/containers. Floors/walls/doors have physical colliders; roof support and shelter checks exist.
- Dismantling now ray-picks the pointed piece, preventing foundation-first selection from making roofs impossible to remove. It rejects supported foundations and full chests, with partial material refund. **Needs live browser verification.** UI still says “nearest” in some instructions; align that text next pass.
- Built-in-imagegen timber, limestone and real-alpha grass joined bark/meadow/leaves. All six PNG originals now live in `art/textures/`, final prompt set `art/textures/prompts.md`; `prepare-assets.mjs` uses local source images and outputs 1024 WebP. External source-image paths are no longer required. Atlas UV flip corrected after actual screenshot showed upside-down grass. 18k candidates now use instanced three-card tufts. Primitive-looking ferns remain a major visual mismatch.
- Blender kit added separate hinged `door`, `satchel`, and harvestable `flax` meshes. Log/stump caps use endgrain material. Timber and rock materials have generated maps and bump. Kit has 26 assets, ~5.37MB optimized GLB.
- Corrected survivor neck crop. Most importantly, inspection proved `Male_Ranger_Arms` already contains full cloth sleeves: the added “Survivor Upper Arms” skin was intersecting them. That extra skin is now retained as a hidden source backup (`asset_family=survivor_skin_backup`) and excluded from export. Non-destructive mesh backups remain. Actual latest screenshot shows continuous green sleeves, not the earlier ragged flesh/clothing patches. Optimized survivor ~3.27MB; all 13 skeletal clips and anatomical sockets retained.
- `.blend` source saved at `assets/source/alderwatch-assets.blend`. Blender claim released. Instance stays `ba12067f-71bd-42eb-bccc-8b2dd0b9dd9f`; list instances before use. Last active scene Characters. `refine_character.py` is idempotent; do not rerun obsolete `export_character.py`.
- Character now uses actual archetype/food stamina maximum and Hunter speed multiplier, .52m step-up for floors, fine-sword visible variant, and outward (-X) transverse tool alignment. Axe/pick contact and two-handed pose still require exact frame-level visual validation.
- Fresh browser test verified a real `sprint` → `run` → `dodge` sequence; dodge was active at 350ms, grounded, and hand/tool origin remained equal. No teleporting or item grants were used.
- Browser walked to camp bench and crafted sword: wood84→81, stone36→34, iron12→8, sword0→1. Walked to cookfire, cooked venison (wood81→80), ate it and verified max health110→145 with timed 480s food buff and recovery. Browser walked to flax22,-12 and gathered exactly4 fiber once. Browser placed a floor at15,-12, material cost6wood/3stone, then duplicate socket correctly rejected.
- Additional live state changes happened outside agent inputs (more gathering, four window walls, another floor, home workbench, extra swords). **The user appears to be playing in the same preview. Do not take over continuously or attribute these observations to agent tests.**
- Starting a new realm now asks and retains prior save in `alderwatch.realm.backup`. Latest read showed a *new hooded Warden*, only harvest-tree progression and nearby physical logs; do not restore the older test realm over this new save. Prior home structures should be in the normal backup if the user started the new realm through the UI.
- Save on pagehide, and development HMR saves/resumes play/build. Existing resource registry is authoritative: generation now only occurs when no resources of that kind exist, instead of inventing missing IDs during every load. Positions and scale remain saved. Fresh trees are spaced at least5.5m. Earlier duplicate resource nodes in an old save are not destructively removed. Mid-fall pose persistence still missing.
- Ground cover is cleared under saved/new floors and stations, and around ambient cottages/stations. Camera ray collision now avoids trunk/wall occlusion. Both need further walk-through QA at the home. Terrain/mountains still look too simple; mountain edge gap reduced but pale horizon band remains visible.
- Latest build passes; tests now cover new station payment/reach, cooking/expiry, support/under-foot rejection, conservation in storage, drops, flax, and expanded assets. 20 tests expected after the final asset regression check.
- `read_realm`, `play_input_sequence`, `read_settlement`, `use_realm_interface`, `read_performance` WebMCP tools exist. Menu/build tool uses current preview and normal authority checks, not cheats. Current input sequence permits only play mode and old movement/equipment/E keys; build/menu actions use the separate tool.
- Performance diagnostic now reports *unclamped* frame FPS, frame interval, CPU update/render submission times, visibility/focus, viewport, draws/triangles/memory. An idle sample ran ~1fps with ~997ms intervals but ~2.4ms CPU time (likely browser scheduling/throttling, not proven GPU failure). A later focused visible sample reported144fps, .30ms update,1.38ms render submission,242draws,1.68mtriangles at1920×1539. **This is not a controlled sustained1080p acceptance pass.** Temporary viewport override has been reset. Main JS ~2.95MB/~1.03MBgzip; chunk warning remains.
- Actual final capture: `art/captures/gameplay-refined-2026-09-06.png`. `settlement-2026-09-06.png` is an earlier title-frame capture despite its name, not evidence of a finished house. No browser error logs at final check.

## Continue next — not complete

1. Implement credible animated bandit AI/combat and enemy drops/camp pressure. **No enemies yet.** Keep authoritative combat rules separate from rendering, stable IDs, actual hit timing/reach, block/dodge/stamina/hit/death. The existing sword clip and verified hand rig are available. Camp chest currently has hide/iron/venison but no guard gating; correct this with combat.
2. Finish and visually test a small house: roof bays orientation, doorway interaction, walk through, floor collisions, vegetation clearing, own workbench/storage, dismantling. Do not overwrite the live user's save. Current old-home observations are partial and do not prove roof/door/storage live behavior.
3. Repeat real screenshot comparisons to concepts. Large remaining mismatches: primitive bright ferns, overly uniform grass patches, flat meadow outside the authored grove, overly faceted/plastic rocks/road, simple mountains with pale horizon seam, flat water, generic cottages/roof/gable details. Improve source UVs: tubes currently use world-coordinate bark mapping and beams use planar projection, creating stretched bark on branches and wrong grain direction on some timbers.
4. Finish creation body/face/hair presets, distinct Artisan/Reaver useful effects, equipment/armor slots, salvage/map/proficiency UI, audio and progression. Generic class differences are only partial. No fake networking.
5. Profile with a focused visible viewport at1080p and test exact axe/pick contact, two-hand placement and feet. Preserve the fixed13clips and anatomical sockets.

## Runtime handoff

Correct project: `C:/Users/jared/Local Sites/alderwatch`. Vite remains exec session **10175** at `http://127.0.0.1:5190/`; verify before starting another. Existing IAB browser1/tab2, providerTabId `c401bf09-a641-41ab-ad26-5917159bb1a1`. It is marked for handoff; reuse it, do not open duplicates. CUA session was reset and its full docs reread; variables currently `tab` (simple), `browser`, `game` (advanced), `gameTools`, `captureFs`. Refetch WebMCP after HMR notifications. Viewport override reset. Blender released.

The prior milestone below is retained as historical context. The newer sections above supersede conflicting implementation status.

# Previous checkpoint — historical, before crafting/building

## Objective, unchanged

Build the commercial-quality Alderwatch medieval survival RPG vertical slice described in `VERTICAL_SLICE.md` and `GAME_VISION.md`. This is **not** the old Blender setup/axe test. The active Codex goal points to `C:/Users/jared/.codex/attachments/4034e6a9-4132-4caf-87cd-6273092a57dd/goal-objective.md`. Do not declare completion from this checkpoint.

## Authoritative progress

- Vite/Three.js/Rapier runtime exists in `src/`. Build passes. `npm test` passes 12 focused state and asset tests.
- Live Vite process: exec session **10175**, URL `http://127.0.0.1:5190/`. Verify the handle before starting another server. IAB browser `1`, tab `2` is the existing preview.
- Title, archetype selection, hair/skin/headwear settings, intro, third-person physics controller, walk/run/sprint/dodge/guard, equipment hotbar and basic inventory are implemented. Archetypes do not yet have distinct actual bonuses; body/face presets remain missing.
- World has textured authored oaks, terrain, stone road, pond, rocks, grass/ferns, modular cottages, workbenches, campfires, chests and a small palisade camp. Most stations currently lack interactions. Mountains were pushed beyond the playable forest after screenshots showed intersecting surfaces. Fresh screenshot confirms they no longer cut through nearby trunks, but their faceted silhouettes/base gaps still look unfinished.
- Blender-authored survivor with verified `hand_r` → `Grip_R`, and left equivalents. Asset optimization preserves empty sockets, distinct material identities, UVs, and the authored `COLOR_1` remapped to GLTF `COLOR_0`.
- Original retargeting was wrong because it read partially updated pose matrices. `scripts/retarget_character.py` fixes it by deriving parent-relative rotations explicitly. Old clips are retained as Blender backups. `scripts/author_tool_clips.py` adds real arm-driven `chop` and `mine` clips. Runtime must visually verify tool contact and two-handed positioning; this is not yet a passed release gate.
- `scripts/refine_character.py` restores exposed shoulder skin, exports survivor and saves source. Shoulder skin still visibly intersects clothing; needs refinement. Runtime grip orientation is calibrated once in reference idle and then remains rigidly bone-local for all animation.
- `src/gathering.ts`: multi-hit axe/pickaxe harvesting, chip pool, notch decals, dynamic Rapier tree falls, physical loose logs/stone/iron, E pickup, persistent drops. No inventory gain on hits. A tree initially became trapped against an overlapping neighbor; fall impulse now chooses clear ground. Need systematic tree-spacing and physical-fall verification.
- Live readback after Continue showed saved player position, gathering proficiency 18, 24 wood, fallen tree state. Screenshot shows loose logs still present. Those are observations, not proof of the entire gathering acceptance sequence.
- Browser performed an input sequence for run/turn/sprint and observed grounded movement and exact socket/tool origin equality. Original Space test exposed transient input loss at high refresh; `Input.endFrame()` now preserves one-shot events until consumed. Dodge still needs a fresh explicit test after that fix.
- Browser reported ~137–144 FPS at 1920×1080, ~170–236 draw calls, ~1.5–1.7 million triangles. This is preliminary and must not be generalized to normal-desktop sustained 60 FPS without further profiling.
- Saved actual screenshot: `art/captures/gameplay-2026-09-06.png`. It is **not** close enough to concept art yet.
- Runtime WebMCP tools `read_realm` and `play_input_sequence` are registered and have been called. An invalid key `not_a_game_input` was rejected with `Invalid bounded input sequence` before mutation. Sequence samples now clone player state to avoid all samples reporting the final mutable state.
- Blender MCP instance `ba12067f-71bd-42eb-bccc-8b2dd0b9dd9f`; source `assets/source/alderwatch-assets.blend`. Claim released. List instances before reconnecting.

## Immediate next work

1. Continue actual screenshot → compare → repair loop. Current world is too sparse, building/materials too simple, rock/road surfaces too flat/faceted, vegetation repetitive, pond flat and cut-out, cut stump has bark mapped over cap. Character clothing/skin intersections and tool contact need close inspection. The tool blade's transverse axis may point inward across the legs: the calibration edge basis currently uses +X while the model's anatomical right hand is on -X. Verify and correct that bone-local orientation. Fix source assets, do not hide problems with dark lighting.
2. Correct source stump/log cap materials and add weathered timber/stone detail. Improve mature-tree spacing and silhouettes. Do not replace authored assets with runtime primitive stand-ins.
3. Implement actual crafting/cooking station interactions, food buffs, snap construction with valid/invalid preview, rotation, dismantling, workbench, storage and shelter. There is data scaffolding, but no playable crafting/building UI yet. `B` and `C` do not currently open those systems.
4. Add credible animated bandit AI/combat, drops and lootable camp; weapon progression; return-home flow. No enemies currently exist in the live scene.
5. Complete creation presets, equipment changes, inventory move/drop/store/salvage, map, skills, progression, settings persistence, soundscape and meaningful audio.
6. Harden persistence: existing resource positions must stay authoritative when world generation changes; preserve fall transforms when saving mid-fall. Starting a new game needs overwrite confirmation or a separate save slot. Realm rebuild now correctly recreates scene/physics against the chosen saved state.
7. Add systematic browser tests, actual contact/foot-motion visual verification, profiling/LOD/culling and compressed geometry as appropriate. Build warnings still flag the ~2.9 MB main JS bundle (includes Rapier WASM payload).

## Assets and commands

- `npm run dev`, `npm run build`, `npm test` (explicit test filenames for Windows compatibility).
- `node scripts/prepare-assets.mjs` generates WebP textures, removes irrelevant scene metadata, deduplicates and resamples GLBs. Inputs `assets/source/survivor.raw.glb`, `assets/source/frontier-kit.raw.glb`; outputs `public/assets/`. Survivor ~3.3 MB, kit ~5.2 MB.
- `build_environment.py` rebuilds only `ALDERWATCH_KIT`, exports raw kit and saves source. Do not accidentally rerun it while unrelated edits overlap the kit. A prior stale-object error occurred **after** successful regeneration/export; inspect state before retrying.
- `retarget_character.py` → `author_tool_clips.py` → `refine_character.py` → `prepare-assets.mjs` is the current character bake/export sequence. Do not rerun the original `export_character.py`: it expects the old head name and implements the broken retargeting algorithm.
- Three concept targets: `art/concepts/hero.png`, `gathering.png`, `settlement.png`.
- Licensed CC0 Quaternius source archives/models/animations under `assets/source/`. Credits already mention Quaternius.
- Generated bark/meadow/leaves images and concept source paths are in `scripts/prepare-assets.mjs` and `art/concepts/prompts.md`. Existing image-only agent `concept_art` is not doing game-code work.

Keep the complete goal active. The full opening-ten-minute acceptance sequence is not finished or verified.
