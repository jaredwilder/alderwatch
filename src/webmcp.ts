type Tool={name:string;description:string;inputSchema:object;annotations?:{readOnlyHint?:boolean};execute:(input:unknown)=>unknown|Promise<unknown>};
export function registerGameTools(tools:Tool[]){const context=(document as Document&{modelContext?:{registerTool:(tool:Tool,options:{signal:AbortSignal})=>Promise<void>|void}}).modelContext;if(!context)return;const life=new AbortController();for(const tool of tools){try{void Promise.resolve(context.registerTool(tool,{signal:life.signal})).catch(console.error);}catch(e){console.error(e);}}window.addEventListener('pagehide',()=>life.abort(),{once:true});}
