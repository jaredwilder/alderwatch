export class Soundscape {
 context?:AudioContext;master?:GainNode;volume=.35;noise?:AudioBuffer;
 start(){if(this.context){void this.context.resume();return;}const c=this.context=new AudioContext();this.master=c.createGain();this.master.gain.value=this.volume;this.master.connect(c.destination);const n=c.createBuffer(1,c.sampleRate*3,c.sampleRate),d=n.getChannelData(0);let last=0;for(let i=0;i<d.length;i++){last=(last+Math.random()*.04-.02)/1.015;d[i]=last;}this.noise=n;const wind=c.createBufferSource();wind.buffer=n;wind.loop=true;const f=c.createBiquadFilter();f.type='lowpass';f.frequency.value=420;const g=c.createGain();g.gain.value=.17;wind.connect(f).connect(g).connect(this.master);wind.start();}
 setVolume(v:number){this.volume=v;if(this.master)this.master.gain.value=v;}
 combat(kind:'swing'|'hit'|'block'){if(kind==='block'){this.impact('metal');return;}if(!this.context||!this.master||!this.noise)return;const c=this.context,t=c.currentTime,s=c.createBufferSource(),f=c.createBiquadFilter(),g=c.createGain();s.buffer=this.noise;f.type='bandpass';f.frequency.setValueAtTime(kind==='swing'?1900:420,t);f.frequency.exponentialRampToValueAtTime(kind==='swing'?380:100,t+.18);g.gain.setValueAtTime(.01,t);g.gain.linearRampToValueAtTime(kind==='swing'?.85:2,t+.035);g.gain.exponentialRampToValueAtTime(.001,t+.24);s.connect(f).connect(g).connect(this.master);s.start(t);s.stop(t+.25);if(kind==='hit')this.impact('wood');}
 impact(kind:'wood'|'stone'|'step'|'metal'){if(!this.context||!this.master||!this.noise)return;const c=this.context,t=c.currentTime,src=c.createBufferSource();src.buffer=this.noise;const f=c.createBiquadFilter();f.type='bandpass';f.frequency.value=kind==='wood'?850:kind==='stone'?1700:kind==='metal'?3200:310;f.Q.value=.7;const g=c.createGain();g.gain.setValueAtTime(kind==='step'?.3:1.3,t);g.gain.exponentialRampToValueAtTime(.001,t+.22);src.connect(f).connect(g).connect(this.master);src.start(t);src.stop(t+.3);const o=c.createOscillator(),og=c.createGain();o.type='triangle';o.frequency.setValueAtTime(kind==='metal'?740:kind==='wood'?110:65,t);o.frequency.exponentialRampToValueAtTime(40,t+.16);og.gain.setValueAtTime(kind==='step'?.07:.2,t);og.gain.exponentialRampToValueAtTime(.001,t+.2);o.connect(og).connect(this.master);o.start(t);o.stop(t+.25);}
}
